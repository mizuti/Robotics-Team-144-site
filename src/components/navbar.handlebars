    <nav class="navbar">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <a class="navbar-brand" href="index.html#page-top">Team<br />144</a>
            </div>
            <div class="navbar-nav right">
                <ul class="nav">
            		{{#each routes}}
                        <li>
                            <a class="nav-link" href={{this.href}} id="homeLink">{{this.text}}</a>
                        </li>
		            {{/each}}
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
